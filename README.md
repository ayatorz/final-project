# final-project

## NNプログラムの実行方法

`music_artist_dataset_template.csv`を使って、歌詞行からアーティストを分類するニューラルネットワークを学習し、短い歌詞風テキストの生成と分類器による判定を行います。
データは同じ曲が複数の分割に混ざらないように、アーティストごとに曲単位で`train:valid:test = 3:1:1`へ分割します。

```bash
source .venv/bin/activate
python3 -m pip install -r requirements.txt
python3 nn_music_project.py --use-title
```

主な出力は`outputs/`に保存されます。

- `artist_classifier.pkl`: 学習済み分類モデル
- `split_summary.json`: 学習用、検証用、テスト用に入った曲名一覧
- `splits/train.csv`, `splits/valid.csv`, `splits/test.csv`: 分割後のデータ
- `classification_metrics.json`: 正解率、混同行列、誤分類例
- `generated_texts.json`: 生成した短文
- `generated_predictions.json`: 生成文を分類器に入力した結果

## うんちち


このプログラムの生成器（N-gram）は、「学習したデータを確率的に繋ぎ合わせているだけ」なので、学習データに存在するフレーズがそのまま出てきたり、同じパターンばかり繰り返したりするのは当然の挙動です。これは、今のコードが「学習データ内の出現頻度（カウント）」をそのまま重みにしているため、確率の高い（＝元の歌詞に多い）並びが選ばれやすくなっているからです。

これでは「生成AI」としては不十分ですね。「貸し（歌詞）」そのままでなく、「新しい文章」を生み出すには、どう工夫すれば良いか、3つの改善策を提案します。

1. 「温度（Temperature）」パラメータを大胆に上げる
コード中の --temperature を変えてみてください。現在 0.9 ですが、これを 1.5 や 2.0 に上げると、確率の低い文字も選ばれるようになり、ありきたりな繋ぎ合わせが減ります。ただし、上げすぎると日本語として崩壊します。

2. 生成モデルを N-gram から「GRU（RNN）」に変える（重要）
実は、これまでの授業で作成した pred_chars_shakes_GRU.py の方が、N-gramよりも遥かに賢く歌詞を生成できます。

N-gramの限界: 「直前の4文字（order=4）」しか見ていないため、文章の全体的な意味（「最初の方はこんな話だった」という文脈）を覚えていられません。

GRUの強み: 内部状態（Hidden State）を持つため、「過去の数百文字分の文脈」を記憶できます。これなら、歌詞の冒頭の内容に沿った、より自然で新しい文章を生成できる確率がグッと上がります。

3. 「サンプリング戦略」を工夫する
今のコードの generate 関数では、重みに基づいてランダムに次の一文字を選んでいますが、ここに 「Top-k サンプリング」 という手法を混ぜると劇的に改善します。

やり方: 確率の高い順に「上位10個の候補」だけを絞り込み、その中から抽選するようにします。

効果: 確率が極端に低いデタラメな文字が選ばれるのを防ぎつつ、確率の高い文字ばかり選ばれる「ありきたりな生成」も回避できます。

具体的なアクションプラン
もし「今のコードを活かしたい」なら、generate 関数の中に「Top-k サンプリング」を実装するのが一番の近道です。

Python
# 現在のサンプリング部分をこれに差し替えるイメージ
candidates = sorted(zip(chars, weights), key=lambda x: x[1], reverse=True)[:10] # 上位10個に絞る
chars, weights = zip(*candidates)
# ここから先は現在のrandom.choicesに渡す
ただ、「もっとクリエイティブで歌詞っぽいものを生成したい」のであれば、今のN-gram（統計的生成）を捨てて、分類器と同じく学習済みのGRUモデルを生成器として使う構成に改造することを強く勧めます。

今のコードの「分類器」は非常に優秀なので、そこに「RNNベースの生成」を組み合わせるのが、3人のプロジェクトとして最も評価される道です。